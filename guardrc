# require 'pry'
# require(@f) if File.exists?(@f='/Users/keyvan/Code/guardrc/lib/guardrc.rb')
# module Guardrc
#   def self.use(*words)
#     # words.each do |word|
#     #   word.to_sym
#     # end
#     syms = words.map(&:to_sym)
#     # binding.pry
#     # eval Guardrc.snippet
#   end
#   def self.parse(sh)
#     if sh.is_a? Array
#       puts "its an array"
#     elsif sh.is_a? Hash
#       puts "its a hash"
#       sh.each do |k,v|
#         puts "key is #{k}, value is #{parse(v)}"
#       end
#     elsif sh.is_a?(Symbol) || sh.is_a?(String)
#       puts "Symbol or String"
#       puts sh
#       puts Guardrc.snippet[sh]
#     end
#   end
#   def self.[](*sniphash)
#     sniphash.each do |h|
#       parse(h)
#     end
#   end
# end


# # eval Guardrc::Snippet::Notification.off

# binding.pry

# Guardrc[
#   notification: :off,
#   testing: [:rspec, :cucumber]
# ]
# eval Guardrc::Snippet::Testingtools.cucumber_and_rspec

# I can do normal Guard::Dsl stuff too
notification :off

# And I can use snippets now


# And any other ruby ... now I can keep file this in the .gitignore and customize
# while still retaining the actual Guardfile