guard 'cucumber'
guard 'rspec'
eval Guardrc.load(File.absolute_path('guardrc')) if File.exists?(f='/Users/keyvan/Code/guardrc/lib/guardrc.rb') && require(f) # this would be required via bundle exec
# and then just do something like Guardrc.load or something anywhere you want to do it